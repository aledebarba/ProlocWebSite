import React from 'react'

const placeholder = (props) => (

    <div style={{
        width:'100%',
        backgroundColor:'gray',
        padding:'20px',
        margin:'20px',
        color:'white'
    }}>
    {props.title}
    </div>
)

export default placeholder
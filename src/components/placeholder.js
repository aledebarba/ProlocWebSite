import React from 'react'

const placeholder = (props) => (

    <div style={{
        display: 'block',
        backgroundColor:'gray',
        padding:'20px',
        margin:'30px 20px',
        color:'white'
    }}>
    {props.title}
    </div>
)

export default placeholder